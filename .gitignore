# Godot 4
game/.godot/
game/.import/
game/android/build/
game/export_presets.cfg.local

# Exports / builds
build/
*.apk
*.aab
*.ipa
export.cfg.local

# Signing secrets — NEVER commit keystores or their passwords.
# export_presets.cfg IS committed (no secrets); the .local overlay is not.
*.keystore
*.jks
keystore.properties

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs / tmp
*.log
/tmp/
